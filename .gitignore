# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Vercel
.vercel

# Claude Code local state (scheduled tasks etc.)
.claude/

# Lokale SQLite-Reste (vor dem Postgres-Umbau)
data/
*.sqlite
*.sqlite-journal
*.db

# Lokale Datei-Uploads (Dev-Fallback fuer Portrait-Upload ohne Vercel-Blob-Token)
public/uploads/

# Bibliothek-PDFs: liegen produktiv in Vercel Blob, nicht im Repo (315 MB).
# Lokal koennen sie hier liegen (fuer Upload-Skript und Test-Uebersetzungen).
pdfs/*.pdf
pdfs/.translations/
